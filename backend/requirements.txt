# Core transcription engine (pulls in CTranslate2, PyAV, onnxruntime for Silero VAD,
# tokenizers and huggingface_hub for the one-time model download).
faster-whisper>=1.2.0,<2
ctranslate2>=4.5.0,<5

# Local HTTP API (bound to 127.0.0.1 only)
fastapi>=0.115,<1
uvicorn>=0.30,<1

# PDF export with real Arabic shaping (HarfBuzz) and bidi support
fpdf2>=2.8.0,<3
uharfbuzz>=0.40

# Free offline Arabic → English translation (Opus-MT model run by CTranslate2)
sentencepiece>=0.2

# RAM / CPU detection and parent-process watchdog
psutil>=5.9
numpy>=1.24

# "Insert into database" feature — pure pip drivers, no database client install needed
pymssql>=2.3          # SQL Server (fallback when no ODBC driver is installed)
pyodbc>=5.1           # SQL Server via "ODBC Driver 17/18" (Windows authentication)
oracledb>=2.0         # Oracle (thin mode, no Instant Client)
PyMySQL>=1.1          # MySQL / MariaDB
psycopg[binary]>=3.1  # PostgreSQL

# YouTube links: read existing captions or download the audio only.
# yt-dlp[default] includes yt-dlp-ejs; deno is the JavaScript runtime YouTube now requires.
# YouTube changes often — keep yt-dlp current with `npm run update:youtube`.
yt-dlp[default]>=2026.8.19
deno>=2.3
